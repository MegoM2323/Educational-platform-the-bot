# PostgreSQL Client Authentication Configuration (Replica/Standby)
# Replica server authentication setup

# TYPE  DATABASE        USER            ADDRESS                 METHOD
# ====  ========        ====            =======                 ======

# Local connections
local   all             all                                     trust

# Replication connections (if this replica becomes primary)
host    replication     replicator      0.0.0.0/0               md5
host    replication     replicator      ::/0                    md5

# IPv4 local connections
host    all             all             127.0.0.1/32            md5

# IPv6 local connections
host    all             all             ::1/128                 md5

# Application read-only connections to replica
host    all             all             0.0.0.0/0               md5

# Docker network connections
host    all             all             172.16.0.0/12           md5

# Kubernetes network connections
host    all             all             10.0.0.0/8              md5

# Patroni connections
host    all             all             127.0.0.1/32            trust
