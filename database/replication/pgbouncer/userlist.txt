# PgBouncer User List
# Format: "username" "password"
# Passwords should be MD5 hashed or use SCRAM-SHA-256

# Application users
"thebot_user" "md5_hash_of_TheBot123!"
"admin" "md5_hash_of_AdminPassword123!"

# PgBouncer admin user
"pgbouncer" "md5_hash_of_PgBouncer123!"

# Replication user
"replicator" "md5_hash_of_ReplicatorPassword123!"

# Read-only user (for replicas)
"read_only" "md5_hash_of_ReadOnly123!"

# Monitoring user
"monitoring" "md5_hash_of_Monitoring123!"

# Generate MD5 hashes using:
# echo -n 'password' | md5sum
# Or use: python -c "import hashlib; print(hashlib.md5(b'password').hexdigest())"
