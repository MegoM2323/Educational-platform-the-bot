# 🎓 THE BOT Platform

Современная образовательная платформа нового поколения с персонализированным обучением, трекингом прогресса и прямой связью между всеми участниками образовательного процесса.

## ✨ Особенности

### 🎯 Для Учеников
- 📚 Доступ к образовательным материалам и заданиям
- 📊 Трекер прогресса с геймификацией (баллы, стрики, точность)
- 💬 Чат с преподавателями и тьюторами в реальном времени
- 🔔 Уведомления о проверке домашних заданий
- 📈 Визуализация образовательного прогресса

### 👨‍👩‍👧 Для Родителей
- 📋 Отчёты о прогрессе детей
- 🤝 Связь с тьютором
- 🎯 Доступ к образовательным целям
- 💳 Управление платежами за обучение
- 🔔 Уведомления о важных событиях

### 👨‍🏫 Для Преподавателей
- 📝 Создание и публикация образовательных материалов
- ✅ Проверка домашних заданий
- 📊 Формирование отчётов по предмету
- 💬 Ответы на вопросы учеников
- 📚 Управление предметами и заданиями

### 🎓 Для Тьюторов
- 👥 Управление группой учеников
- 📄 Создание финальных отчётов
- 🤝 Координация с преподавателями
- 💬 Консультации родителей
- ➕ Создание новых учётных записей для учеников и родителей

## 🛠 Технологический стек

### Backend
- **Django 5.2+** — веб-фреймворк
- **Django REST Framework** — RESTful API
- **Django Channels** — WebSocket поддержка для реального времени
- **PostgreSQL / SQLite** — база данных (Supabase для продакшена)
- **Redis** — кэширование и очереди задач
- **Celery** — асинхронные задачи
- **Daphne** — ASGI сервер для WebSocket

### Frontend
- **React 18** — UI библиотека
- **TypeScript** — типизированный JavaScript
- **Vite** — современный сборщик
- **React Router** — маршрутизация
- **TanStack Query** — управление состоянием сервера
- **shadcn/ui + Radix UI** — компоненты интерфейса
- **Tailwind CSS** — стилизация
- **React Hook Form + Zod** — формы и валидация

### Интеграции
- **Supabase** — аутентификация и база данных
- **Telegram Bot API** — уведомления
- **YooKassa** — платежная система
- **WebSocket** — реальное время для чата

## 📁 Структура проекта

```
THE_BOT_platform/
├── backend/                 # Django приложение
│   ├── accounts/            # Управление пользователями и профилями
│   ├── applications/        # Заявки на обучение
│   ├── assignments/         # Домашние задания
│   ├── chat/                # Система чата (WebSocket)
│   ├── materials/           # Образовательные материалы
│   ├── notifications/       # Система уведомлений
│   ├── payments/            # Платежная система
│   ├── reports/             # Отчёты и аналитика
│   ├── core/                # Общие утилиты и мониторинг
│   ├── config/              # Настройки Django
│   └── requirements.txt     # Python зависимости
│
├── frontend/                # React приложение
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── pages/           # Страницы приложения
│   │   ├── hooks/           # React хуки
│   │   ├── services/        # API сервисы
│   │   ├── utils/           # Утилиты
│   │   └── types/           # TypeScript типы
│   ├── public/              # Статические файлы
│   └── package.json         # Node зависимости
│
├── tests/                   # Тесты (Playwright, pytest)
├── start.sh                 # Скрипт запуска платформы
└── env.example              # Пример конфигурации
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10+
- Node.js 18+
- PostgreSQL (опционально, для продакшена)
- Redis (опционально, для кэширования)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/MegoM2323/Educational-platform-the-bot.git
cd THE_BOT_platform
```

2. **Настройте виртуальное окружение Python:**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. **Установите зависимости бэкенда:**
```bash
cd backend
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cd ..
cp env.example .env
# Отредактируйте .env файл и добавьте необходимые ключи
```

5. **Примените миграции:**
```bash
cd backend
python manage.py migrate
```

6. **Создайте суперпользователя (опционально):**
```bash
python manage.py createsuperuser
```

7. **Установите зависимости фронтенда:**
```bash
cd ../frontend
npm install
```

### Запуск

#### Автоматический запуск (рекомендуется)

Используйте скрипт `start.sh` для автоматического запуска всех сервисов:

```bash
chmod +x start.sh
./start.sh
```

Скрипт автоматически:
- Освобождает порты 8000 и 8080
- Устанавливает зависимости
- Применяет миграции
- Запускает Django сервер (порт 8000)
- Запускает React сервер (порт 8080)

#### Ручной запуск

**Backend:**
```bash
cd backend
source ../.venv/bin/activate
python manage.py runserver 8000
```

**Frontend:**
```bash
cd frontend
npm run dev -- --port 8080
```

После запуска:
- 🌐 Frontend: http://localhost:8080
- 🔧 Backend API: http://localhost:8000/api/
- 👨‍💼 Admin панель: http://localhost:8000/admin

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта на основе `env.example`:

```env
# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=['127.0.0.1', 'localhost']

# Telegram Bot Settings
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_channel_id_here

# Supabase Settings
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# YooKassa Settings
YOOKASSA_SHOP_ID=your_shop_id_here
YOOKASSA_SECRET_KEY=your_secret_key_here
YOOKASSA_WEBHOOK_URL=your_webhook_url_here

# Frontend Settings
VITE_DJANGO_API_URL=http://localhost:8000/api

# Cache Settings
USE_REDIS_CACHE=False
REDIS_URL=redis://127.0.0.1:6379
```

### Настройка Supabase

1. Создайте проект на [Supabase](https://supabase.com)
2. Получите URL и ключи API
3. Добавьте их в `.env` файл
4. Примените миграции из `frontend/supabase/migrations/`

### Настройка Telegram Bot

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Создайте канал и добавьте бота как администратора
4. Получите ID канала (начинается с `-100`)
5. Добавьте токен и ID в `.env` файл

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/login/` — вход
- `POST /api/auth/logout/` — выход
- `POST /api/auth/register/` — регистрация

### Ученики
- `GET /api/student/materials/` — список материалов
- `GET /api/student/dashboard/` — дашборд ученика
- `POST /api/assignments/submit/` — отправка задания

### Преподаватели
- `GET /api/teacher/materials/` — управление материалами
- `POST /api/teacher/materials/create/` — создание материала
- `GET /api/teacher/assignments/` — список заданий
- `POST /api/teacher/assignments/check/` — проверка задания

### Тьюторы
- `GET /api/tutor/students/` — список учеников
- `POST /api/tutor/students/create/` — создание ученика
- `GET /api/tutor/reports/` — отчёты

### Родители
- `GET /api/dashboard/parent/` — дашборд родителя
- `GET /api/reports/parent/` — отчёты о детях

### Чат
- `GET /api/chat/rooms/` — список комнат
- `POST /api/chat/messages/` — отправка сообщения
- `WebSocket /ws/chat/` — WebSocket для реального времени

### Платежи
- `POST /api/payments/create/` — создание платежа
- `GET /api/payments/status/` — статус платежа
- `POST /yookassa-webhook` — webhook от YooKassa

Полная документация API доступна после запуска на `/api/` (если настроен Swagger/ReDoc).

## 🧪 Тестирование

### Backend тесты (pytest)
```bash
cd backend
pytest
```

### Frontend тесты (Playwright)
```bash
cd frontend
npx playwright test
```

### E2E тесты
```bash
cd tests
python test_e2e_integration.py
```

### Все тесты
```bash
cd tests
python run_integration_tests.py
```

## 🏗 Разработка

### Структура приложений Django

- **accounts** — управление пользователями, ролями, профилями
- **applications** — система заявок на обучение
- **assignments** — домашние задания
- **chat** — система чата с WebSocket поддержкой
- **materials** — образовательные материалы и предметы
- **notifications** — система уведомлений
- **payments** — интеграция с платежными системами
- **reports** — генерация отчётов и аналитика
- **core** — общие утилиты, мониторинг, бэкапы

### Frontend архитектура

- **React Router** — маршрутизация с защищёнными роутами
- **TanStack Query** — управление состоянием сервера
- **Context API** — глобальное состояние (Auth, Error Handling)
- **Custom Hooks** — переиспользуемая логика
- **Service Layer** — абстракция API запросов

### Команды разработки

**Backend:**
```bash
# Создать миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Сборка статики
python manage.py collectstatic
```

**Frontend:**
```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview

# Линтинг
npm run lint
```

## 🚢 Деплой

### Production настройки

1. Установите `DEBUG=False` в `.env`
2. Настройте `ALLOWED_HOSTS` с вашим доменом
3. Используйте PostgreSQL вместо SQLite
4. Включите Redis кэширование (`USE_REDIS_CACHE=True`)
5. Настройте HTTPS (SSL сертификаты)
6. Используйте Gunicorn/Daphne для ASGI

### Docker (опционально)

Создайте `Dockerfile` и `docker-compose.yml` для контейнеризации.

### Рекомендации

- Используйте Nginx как reverse proxy
- Настройте мониторинг (Django monitoring в `core/monitoring.py`)
- Регулярные бэкапы базы данных
- Настройте логирование
- Используйте CDN для статики

## 📝 Лицензия

ISC

## 👥 Авторы

- [MegoM2323](https://github.com/MegoM2323)

## 🤝 Вклад в проект

Приветствуются любые вклады! Пожалуйста, создавайте issue или pull request.

## 📧 Контакты

- GitHub: [Educational-platform-the-bot](https://github.com/MegoM2323/Educational-platform-the-bot)
- Issues: [GitHub Issues](https://github.com/MegoM2323/Educational-platform-the-bot/issues)

---

**THE BOT Platform** — Образовательная платформа нового поколения 🚀

