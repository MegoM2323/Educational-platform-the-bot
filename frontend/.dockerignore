# ============================================================================
# Docker Build Essentials - DO NOT IGNORE
# ============================================================================
# These files are REQUIRED for Docker build and must NOT be excluded:
# - src/              : Source code directory with React components
# - public/           : Static assets served by web server
# - vite.config.ts    : Build tool configuration for optimal bundling
# - tsconfig.json     : TypeScript root configuration
# - tsconfig.app.json : App-specific TypeScript settings
# - tsconfig.node.json: Node/build tool TypeScript settings
# - package.json      : Dependencies and scripts (implicitly needed)
# - package-lock.json : Locked dependency versions
# ============================================================================

# ============================================================================
# Version Control (safe to exclude - not needed in runtime container)
# ============================================================================
# Git history and configuration files are development artifacts
# Container only needs source code, not git metadata
.git
.gitignore
.gitattributes
.hg
.svn

# ============================================================================
# Node Modules (will be freshly installed in Docker)
# ============================================================================
# Excluding node_modules ensures:
# - Clean, verified dependencies installed via package-lock.json
# - Prevents platform-specific binaries from host contaminating container
# - Reduces build context size (improves build speed)
node_modules/

# ============================================================================
# Build Output (will be regenerated in Docker)
# ============================================================================
# These are created during build process and don't need to be copied
# Excluding reduces context size and ensures fresh builds
dist/
build/

# ============================================================================
# Development Log Files (no value in production container)
# ============================================================================
# Package manager debug logs from host machine
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================================================
# IDE and Editor Configuration (development-only)
# ============================================================================
# IDE config files contain local machine paths and preferences
# Not needed in container, can conflict with different environments
.vscode
.idea
*.swp
*.swo
*~
.DS_Store
.sublime-project
.sublime-workspace
.project
.pydevproject
.settings/
*.iml
.prettierrc
.eslintrc

# ============================================================================
# Testing Files and Coverage (not needed in production container)
# ============================================================================
# Test code and coverage reports are development artifacts
# Production container only needs source code
coverage/
.coverage
.nyc_output/
__tests__/
*.test.js
*.spec.js
jest.config.js
vitest.config.ts
playwright.config.ts
tests/

# ============================================================================
# Environment Variables (sensitive data, never copy from host)
# ============================================================================
# Local .env files contain secrets and machine-specific configs
# Use Docker secrets or build args instead
.env
.env.local
.env.*.local

# ============================================================================
# Temporary Files (cleanup artifacts)
# ============================================================================
# Leftover files from various tools, no value in container
tmp/
temp/
*.tmp
.tmp

# ============================================================================
# Operating System Files (cross-platform garbage)
# ============================================================================
# Platform-specific metadata that pollutes containers
Thumbs.db
.DS_Store
._*

# ============================================================================
# Application Logs (production logs go to stdout/stderr)
# ============================================================================
# Don't bundle host logs into container
logs/
*.log

# ============================================================================
# Docker Configuration Files (not needed in image)
# ============================================================================
# Dockerfile and docker-compose are build instructions, not runtime data
# .dockerignore itself is not needed in the image
Dockerfile*
docker-compose*.yml
.dockerignore

# ============================================================================
# CI/CD Configuration (not needed in production)
# ============================================================================
# GitHub Actions, GitLab CI, CircleCI configs are for pipeline only
.github/
.gitlab-ci.yml
.circleci/
jenkins/

# ============================================================================
# Documentation (optional, keep for reference in dev)
# ============================================================================
# Usually not needed in production container, reduces image size
docs/
README.md
LICENSE
CONTRIBUTING.md
CHANGELOG.md

# ============================================================================
# Development Tools Configuration (not critical for runtime)
# ============================================================================
# Editor config and prettier ignore are nice-to-have but not required
.editorconfig
.prettierignore

# ============================================================================
# Miscellaneous Development Artifacts
# ============================================================================
# Backup files and example configs not needed in production
*.orig
.env.example
Makefile
